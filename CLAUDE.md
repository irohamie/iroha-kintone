# CLAUDE.md

このファイルは Claude Code が毎セッション自動で読み込む永続指示です。
作業を始める前に、必ずこのファイル全体を読んでください。

---

## 1. このリポジトリの目的

NPO法人いろ葉（三重県、児童発達支援事業所）が運用する kintone（`iroha-mie.cybozu.com`）の
アプリ設定とJS/CSSカスタマイズコードを、Gitで版管理するためのリポジトリです。

管理者：齋藤眞寛（まーくん）／代表理事
主な操作端末：iPad、Androidスマートフォン（PCは常用しない）

## 2. 参照文書

| ファイル | 内容 |
| --- | --- |
| `docs/01_設計図.md` | 構造・認証方式・安全性設計・デプロイゲート・禁止事項 |
| `docs/02_手順書.md` | Phase 0〜7の実作業手順 |
| `docs/03_コード生成規約.md` | コード生成のルール |
| `prompts/*.md` | Phase別の作業指示（まーくんはこのファイル名だけを指示に使う） |

矛盾を見つけた場合は、実装を進めず、文書の更新を提案してください。

---

## 3. まーくんからの指示の受け取り方

まーくんはスマートフォンから操作するため、長文を貼り付けません。
**「prompts/01_Phase0_認証検証.md の指示に従って作業してください」のような1行だけが届きます。**

その場合、あなたは以下の順で動いてください。

1. 指定された `prompts/` 配下のファイルを読む
2. このCLAUDE.mdと`docs/01_設計図.md`と`docs/03_コード生成規約.md`を読む
3. 作業内容を要約して、着手前にまーくんへ1回だけ確認する
4. 承諾を得たら実装する

【禁止】prompts のファイルを読まずに推測で作業を始めること。

---

## 4. 実行体制（役割分担）【2026年7月28日 確定】

**確定事項：Claude Code の実行環境から kintone（iroha-mie.cybozu.com）へは
到達できません。** egressポリシーにより CONNECT 自体が拒否されることを実測で確認済みです
（`x-deny-reason: host_not_allowed` および `connect_rejected` の両方の表現で同一現象を確認）。

そのため、kintoneへの実通信は**すべて GitHub Actions のランナー**が担当します。
Claude Code は「コードを書く」「GitHub Actions を起動する」「実行結果を取得して解析する」
役割に専念します。

| 担い手 | 役割 | kintoneへの直接通信 |
| --- | --- | --- |
| Claude Code | コードを書く、GitHub Actions を起動する、ログを取得し解析する、原因を特定する、コミット・pushする | 行わない（できない） |
| GitHub Actions | Secretsを使ってkintoneと実通信する | 行う |
| まーくん | 方針決定、kintone画面での目視確認、**本番デプロイの最終承認のみ** | 行わない |

### 4-1. Claude Code は GitHub Actions を自分で起動・監視する

このリポジトリに連携している GitHub App「Claude」には、
Actions・code・workflows に対する読み書き権限が付与済みです。
この権限を使い、**まーくんに「Actionsタブを開いてRun workflowを押してください」
と依頼しないこと。** 代わりに、あなた自身が以下を行ってください。

1. GitHub API（`gh` コマンドが使えるならそれを、無ければ REST API を直接）で
   対象ワークフローに `workflow_dispatch` イベントを送って起動する
   ```
   POST /repos/irohamie/iroha-kintone/actions/workflows/{workflow_id}/dispatches
   body: { "ref": "main", "inputs": { ... } }
   ```
2. `GET /repos/irohamie/iroha-kintone/actions/runs?event=workflow_dispatch` を
   数秒間隔でポーリングし、起動した実行を見つける
3. `status` が `completed` になるまで待つ
4. `GET /repos/irohamie/iroha-kintone/actions/runs/{run_id}/jobs` でジョブ結果を取得
5. 失敗している場合、`GET /repos/irohamie/iroha-kintone/actions/jobs/{job_id}/logs` で
   ログ本文を取得して原因を解析する
6. 結果を日本語で要約してまーくんに報告する

**まーくんにブラウザのF12・リモートデスクトップ・Actionsタブでの手動操作をさせないこと。**
唯一の例外は、GitHub Environments の **Required reviewers 承認**
（Phase 5以降の本番デプロイ直前）で、これは意図的な人手の安全ゲートなので残す。

### 4-2. 認証の使い分け（重要）

| 用途 | 使う認証 |
| --- | --- |
| GitHubへのAPI操作（ワークフロー起動・ログ取得・コミット・push） | Claude Code に既に付与されている GitHub App の権限。追加のトークンは不要 |
| kintoneへの通信 | GitHub Actions の実行中にのみ、GitHub Secrets から環境変数として渡される。Claude Code のセッションには一切渡さない |

**Claude Code のセッションに kintone のパスワードを持たせる設計は撤回します。**
`.env` はローカルでの動作確認用として一応用意しますが、Claude Code の実行環境からは
kintoneに到達できないため実質的に使えません。作成不要です。

### 4-1. 資格情報の扱い

kintoneへの接続には、リポジトリ直下の `.env` を使います。このファイルはGit管理外です。

```
KINTONE_BASE_URL=https://iroha-mie.cybozu.com
KINTONE_USERNAME=github-bot
KINTONE_PASSWORD=（まーくんが提供する値）
```

【必須】`.env` が無い場合、まーくんにパスワードの提供を依頼してから作成する。
【必須】`.gitignore` に `.env` が含まれていることを毎セッション確認する。
【禁止】`.env` の中身を `cat` などで画面に表示すること。
【禁止】パスワードを `console.log`、コミットメッセージ、報告文に含めること。
【禁止】`.env` を `git add` すること。作業前に必ず `git status` で混入していないか確認する。

### 4-3. 自動化ユーザーの権限について（重要な認識）

自動化用ユーザー `github-bot` には、対象アプリの「アプリ管理」権限のみを付与し、
レコード閲覧・追加・編集・削除の権限は付与していません。

ただし**アプリ管理権限は、アプリのアクセス権設定を書き換えることで
レコード閲覧権限を自分に付与できる**ため、実質的にはレコードへ到達しうる強い権限です。
このパスワードは児童の個人情報に到達しうる資格情報として扱ってください。

【禁止】`github-bot` のアクセス権設定（acl）を変更するコードを書くこと。
【禁止】レコードAPI（`/k/v1/records.json` 等）を呼ぶこと。このリポジトリの対象外です。

### 4-3-1. 例外：初期設定としてのACL追記（2026年7月28日 確定）【重要】

上記「アクセス権の変更は自動化しない」という原則は、**通常運用（Phase 0〜7）に対して適用される。**
以下の条件をすべて満たす場合に限り、一回限りの例外として自動化を許可する。

**適用条件（すべて満たすこと）**

1. 認証に `github-bot` ではなく、**まーくん本人の資格情報**
   （`TEMP_ADMIN_USERNAME` / `TEMP_ADMIN_PASSWORD`）を使う
2. 目的が「`github-bot` にアプリ管理権限を付与する」という**初期設定のみ**であること
3. 既存の ACL エントリを**1件も変更・削除せず、追記のみ**行うこと
   （送信前後でエントリ数が減っていないことをコードで検証する）
4. 送信前に必ず現在のACLを取得し `_backup_before/` 相当の場所に退避すること
5. 対象を明示的に列挙したリストに限定し、リスト外のアプリには一切触れないこと
6. 作業完了後、`TEMP_ADMIN_USERNAME` / `TEMP_ADMIN_PASSWORD` を
   GitHub Secretsから削除するようまーくんに明示的に案内すること
7. レコードAPI、レコードの権限（`recordViewable` 等）には一切触れないこと
   （追記するのは `appEditable: true` のみ。他の権限フィールドはすべて `false`）

**この例外がなぜ許容されるか**：ACLの変更（追記のみ）は、フィールド削除やアプリ削除と違い
**不可逆ではない**。変更前のACLを退避してあるため、問題があれば送り直すだけで元に戻せる。
危険なのは「データが失われる」ことであり、「一時的に強い権限を使うこと」自体ではない。

この条件を1つでも満たさない変更（例：レコード権限の変更、既存エントリの削除、
対象リスト外への適用）は、この例外の対象外とし、引き続き禁止する。

**この例外は初期設定作業にのみ適用され、Phase 0〜7 の通常運用には適用されない。**
Phase 0〜7 では引き続き `github-bot` のみを使い、ACLには一切触れないこと。

---

## 5. 技術スタックの制約

| 項目 | 決定 | 理由 |
| --- | --- | --- |
| 言語 | Node.js（CommonJS、`require`形式） | GitHub Actionsで追加設定なく動く |
| Nodeバージョン | 20以上 | `fetch` / `FormData` / `Blob` が標準搭載 |
| 外部依存パッケージ | **ゼロ**。`package.json` も作らない | サプライチェーンリスクの排除 |
| HTTP通信 | 標準の `fetch` | 同上 |
| multipart送信 | 標準の `FormData` + `Blob` | `form-data` パッケージ不要 |
| MD5計算 | `require('node:crypto').createHash('md5')` | 標準機能 |
| 構文検証 | `node --check <path>` | 標準機能 |
| 環境変数の読み込み | `.env` を自作パーサで読む（`dotenv`は使わない） | 依存ゼロを維持 |

【禁止】`npm install` を実行すること。`axios`、`node-fetch`、`form-data`、`@kintone/rest-api-client` などを使うこと。

---

## 6. kintone REST API の仕様（実装前に必ず検証すること）

以下は設計時点での理解です。**推測を含むため、実装前に公式ドキュメント
（https://cybozu.dev/ja/kintone/docs/rest-api/ ）を参照して裏を取り、
差異があれば `docs/01_設計図.md` を先に更新してください。**

### 6-1. 認証

```
X-Cybozu-Authorization: <base64("ログイン名:パスワード")>
```

アプリ設定API（`customize.json`、`deploy.json`、`apps.json`、`form/fields.json` など）は
APIトークン認証が使えないため、上記のパスワード認証を使います。

### 6-2. コード管理で使うエンドポイント

| 用途 | メソッド・パス | 危険度 |
| --- | --- | --- |
| アプリ一覧 | `GET /k/v1/apps.json?limit=100&offset=0` | 安全 |
| 運用環境のカスタマイズ設定 | `GET /k/v1/app/customize.json?app={id}` | 安全 |
| テスト環境のカスタマイズ設定 | `GET /k/v1/preview/app/customize.json?app={id}` | 安全 |
| ファイル本体の取得 | `GET /k/v1/file.json?fileKey={key}` | 安全 |
| ファイルのアップロード | `POST /k/v1/file.json`（multipart、フィールド名 `file`） | 設定に影響しない |
| テスト環境の設定更新 | `PUT /k/v1/preview/app/customize.json` | テスト環境のみ変更 |
| 運用環境への反映 | `POST /k/v1/preview/app/deploy.json` | **本番が変わる** |
| 反映状況の確認 | `GET /k/v1/preview/app/deploy.json?apps[0].app={id}` | 安全 |

### 6-3. アプリ設定管理で使うエンドポイント（Phase 7）

| 用途 | メソッド・パス | 危険度 |
| --- | --- | --- |
| フィールド一覧取得 | `GET /k/v1/preview/app/form/fields.json?app={id}` | 安全 |
| フォームレイアウト取得 | `GET /k/v1/preview/app/form/layout.json?app={id}` | 安全 |
| 一覧（ビュー）取得 | `GET /k/v1/preview/app/views.json?app={id}` | 安全 |
| 一般設定取得 | `GET /k/v1/preview/app/settings.json?app={id}` | 安全 |
| アクセス権取得 | `GET /k/v1/preview/app/acl.json?app={id}` | 安全 |
| **新規アプリ作成** | `POST /k/v1/preview/app.json` | 低（既存を壊さない） |
| **フィールド追加** | `POST /k/v1/preview/app/form/fields.json` | 低（既存データに影響しない） |
| **フィールド変更** | `PUT /k/v1/preview/app/form/fields.json` | 中〜高 |
| **フィールド削除** | `DELETE /k/v1/preview/app/form/fields.json` | **極大・不可逆** |
| レイアウト変更 | `PUT /k/v1/preview/app/form/layout.json` | 低 |
| ビュー変更 | `PUT /k/v1/preview/app/views.json` | 中 |
| アクセス権変更 | `PUT /k/v1/preview/app/acl.json` | **高** |

### 6-4. customize.json のレスポンス構造（想定）

```json
{
  "scope": "ALL",
  "desktop": {
    "js": [
      { "type": "URL", "url": "https://..." },
      { "type": "FILE", "file": { "fileKey": "...", "name": "board.js", "contentType": "text/javascript", "size": "137216" } }
    ],
    "css": []
  },
  "mobile": { "js": [], "css": [] },
  "revision": "12"
}
```

注意点：
- `size` は**文字列**で返る。数値比較する際は必ず変換すること
- `scope` は `ALL` / `ADMIN` / `NONE` のいずれか
- 配列の順序がJSの実行順序である。**順序を絶対に変えないこと**

### 6-5. fileKey の扱いについての判断

`customize.json` のGETで返る `fileKey` を、そのまま `PUT` に再利用できるかは**不確実**です。
そのため、**再利用せず、常にローカルのファイルを `POST /k/v1/file.json` で
再アップロードして新しい fileKey を得る方式**を採用します。

理由：バイト列がローカルにあるため常に再アップロード可能で、挙動が決定的になる。
内容が同一でも再アップロードは無害。

---

## 7. 安全性の絶対ルール

### 7-1. Phase順序の厳守

**Phase 2（初回バックアップ）が完了しコミットされるまで、kintoneへの書き込み系API
（`POST /k/v1/file.json`、`PUT .../customize.json`、`POST .../deploy.json`、
`POST/PUT/DELETE .../form/fields.json`）を一切実行してはいけません。**

kintoneにはJSカスタマイズやフィールド設定の「前のバージョンに戻す」機能がありません。
バックアップがない状態での書き込みは、取り返しのつかない事故になります。

### 7-2. PUT は全置き換えである

`PUT /k/v1/preview/app/customize.json` は部分更新ではなく全置き換えです。
3ファイル登録されているアプリに1ファイルだけ送ると、残り2つは警告なしに消えます。

【必須】PUTでは `desktop.js` / `desktop.css` / `mobile.js` / `mobile.css` の
4配列すべてを、`manifest.json` の順序どおりに完全に列挙すること。

### 7-3. revision による楽観ロック

【必須】PUT・POST・DELETE・deploy には常に取得済みの `revision` を明示指定すること。
【禁止】`revision: -1`（チェック無効）を指定すること。

### 7-3-1. 承認ゲート（Gate 5）の意味づけ【2026年7月28日 確定】

まーくんは「この変更が技術的に正しいか」を判断できません。判断できるのは
「これは自分が頼んだ内容と合っているか」だけです。そのためGate 5（`--confirm`）は
**技術的正しさの承認ではなく、意図とのズレが無いかの最終目視**として設計します。

【必須】デプロイ系スクリプト（`07_deploy.js`、`11_deploy_settings.js`）は、
GitHub Environments の承認待ちに入る前に、以下を満たす**日本語の一言サマリ**を
`$GITHUB_STEP_SUMMARY` に出力すること。

```
【今回の変更】App{ID}（{アプリ名}）
変更内容：{何を、なぜ変えるか。1〜2文の平易な日本語}
ファイル数：{変更前}個 → {変更後}個（削除：{あり/なし}）
これは元の依頼「{まーくんが最初に頼んだ内容の要約}」と一致しています。
```

技術チェック（7つのゲート）はClaude Codeが担当し続ける。人に技術判断を求めない。
承認画面でまーくんが確認するのは「このサマリが自分の依頼と合っているか」のみ。

**承認を軽量化した分、実質的な安全網は「戻せること」に一本化されます。**
Gate 2（デプロイ前退避の存在確認）と Gate 7（デプロイ後のMD5照合）は、
これまで以上に厳密に実装し、省略や緩和を提案しないこと。
問題が起きた際は「_backup_before/{当日}_{folder}/ へ即座に戻せる」ことが
唯一かつ最大の安全装置になる。

### 7-4. preflight の省略禁止

書き込み系スクリプトは、先頭で必ず「運用環境とテスト環境が完全一致していること」を確認します。
一致しない場合は他者の未反映変更が存在するため、**処理を中止して報告**します。

### 7-5. サイズとMD5による検証（過去の事故対策）

過去に `board.js` が 137KB → 118KB に切り詰められた事故があります。

【必須】
1. ダウンロード時、`customize.json` の `size` と実バイト数を突き合わせる。不一致なら処理中止
2. 全ファイルのMD5を `manifest.json` に記録する
3. アップロード後、**取得し直して**MD5を再計算し、送信前と一致することを確認する
4. 人への報告時は必ずファイル名・バイト数・MD5を明記する

### 7-6. 削除禁止ディレクトリ

`_backup_before/` と `_deploy_log/` は追記専用です。中身の削除・上書きを絶対に行わないこと。

### 7-7. バックアップの保持方針

古いバックアップの削除ローテーションは**行いません**。Gitの全履歴を無期限に保持します。
月初・年初には `monthly-{YYYY-MM}-App{ID}` / `yearly-{YYYY}-App{ID}` のタグを自動付与します。

### 7-8. アプリ設定・フィールド操作の禁止事項（Phase 7）

以下は**自動化の対象外**とし、コードを書いてはいけません。
必要な場合はまーくんがkintone管理画面で手作業で行います。

| 操作 | 禁止理由 |
| --- | --- |
| フィールドの削除（`DELETE .../form/fields.json`） | そのフィールドの全レコードのデータが不可逆に失われる |
| フィールドコード（`code`）の変更 | 既存JS・計算式・ルックアップ・関連レコードが一斉に壊れる |
| フィールドの型（`type`）の変更 | データの変換に失敗して欠損する |
| アクセス権の変更（`PUT .../acl.json`、`field/acl.json`、`record/acl.json`） | 児童の個人情報が意図せず露出、または業務が停止する。**ただし4-3-1の条件をすべて満たす初期設定作業（github-botへのアプリ管理権限の追記のみ）は例外として許可する** |
| アプリの削除 | 不可逆 |
| ドロップダウン・ラジオボタンの選択肢の削除 | 既存レコードの値が選択肢から外れて不整合になる |

許可される操作：新規アプリの作成、フィールドの追加、ラベル（`label`）の変更、
選択肢の追加、レイアウトの変更、フォームの並び替え、ビューの追加。

---

## 8. 判断に迷ったときの原則

1. **迷ったら止める。** 書き込み系で判断がつかないときは実行せず、まーくんに質問する
2. **推測でAPIを叩かない。** 仕様が不明なAPIは公式ドキュメントで確認する
3. **設計図と矛盾する実装をしそうになったら、実装ではなく設計図の更新を提案する**
4. **エラーを飲み込まない。** try-catchで握りつぶさず、必ず内容を表示して処理を止める
5. **「動いたからよし」としない。** バイト数とMD5で裏を取るまで完了と報告しない
6. **勝手にPhaseを先に進めない。** 1つのPhaseが終わったら報告し、次の指示を待つ
7. **まーくんに技術的な作業を投げない。** ログ取得・原因調査・再実行はあなたが行う
8. **まーくんは手順書（docs/ や prompts/）を自分では読みません。** これらはあなたが読むための文書です。
   まーくんに何か作業を頼むときは、「docs/02_手順書.mdのX-Xを見てください」のような
   文書参照だけで済ませず、**その場でチャットに手順そのものを書き出す**こと。
   ボタンの名前・タップする位置まで、その都度チャット本文に含めること。

---

## 9. 現在の進捗

| Phase | 内容 | 状態 |
| --- | --- | --- |
| 初期構築 | フォルダ・文書・プロンプト | 完了（2026年7月28日） |
| Phase 0 | 認証検証 | 進行中（パスワード認証は成功。customize.json系のアクセス権付与漏れがApp227/228で疑われ、要確認） |
| Phase 1 | アプリ一覧取得 | 完了（2026年7月28日、config/apps.json生成。66アプリ中App227/228が一覧に含まれず要確認） |
| Phase 2 | 初回バックアップ（コード＋アプリ設定） | 未着手 |
| Phase 3 | 差分検知の自動化 | 未着手 |
| Phase 4 | デプロイ手順の確立（検証用アプリ） | 未着手 |
| Phase 5 | 本番デプロイ運用 | 未着手 |
| Phase 6 | 対象アプリ拡大 | 未着手 |
| Phase 7 | アプリ設定・フィールド管理 | 未着手 |

Phaseを完了したら、この表を更新してコミットしてください。
